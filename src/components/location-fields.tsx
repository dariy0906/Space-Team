'use client';
import { useState } from 'react';
export default function LocationFields() { const [lat,setLat] = useState('43.653'); const [lng,setLng] = useState('51.174'); const [message,setMessage] = useState(''); function locate() { navigator.geolocation?.getCurrentPosition(p => { setLat(String(p.coords.latitude)); setLng(String(p.coords.longitude)); setMessage('Координаты обновлены'); }, () => setMessage('Геолокация недоступна')); } return <><label className="field">Широта<input name="lat" type="number" step="any" value={lat} onChange={e=>setLat(e.target.value)} required/></label><label className="field">Долгота<input name="lng" type="number" step="any" value={lng} onChange={e=>setLng(e.target.value)} required/></label><div className="wide"><button className="button secondary" type="button" onClick={locate}>◎ Использовать моё местоположение</button>{message&&<span className="subtle ml-2">{message}</span>}</div></>; }

import {createReportAction} from '@/app/actions';
import LocationFields from '@/components/location-fields';
import SubmitButton from '@/components/submit-button';
import {requireUser} from '@/lib/auth';
import {reportCategories,typeLabel} from '@/lib/labels';
export default async function ReportForm({searchParams}:{searchParams:Promise<{error?:string}>}){await requireUser('RESIDENT');const {error}=await searchParams;return <><div className="page-heading"><div><p className="eyebrow">НОВОЕ ОБРАЩЕНИЕ</p><h1>Сообщить о проблеме</h1><p className="subtle">Выберите точку на карте и прикрепите фото.</p></div></div>{error&&<p role="alert" className="error-banner">{error}</p>}<div className="panel max-w-3xl"><form action={createReportAction} className="panel-body form-grid"><label className="field">Категория<select name="type">{reportCategories.map(k=><option key={k} value={k}>{typeLabel[k]}</option>)}</select></label><label className="field">Название<input name="title" required minLength={4} maxLength={100}/></label><label className="field wide">Описание<textarea name="description" required minLength={5} maxLength={2000}/></label><label className="field">Адрес<input name="address" required minLength={3}/></label><label className="field">Микрорайон<input name="district"/></label><LocationFields/><label className="field wide">Фото проблемы · JPG, PNG, WebP до 5 МБ<input name="photo" type="file" required accept="image/jpeg,image/png,image/webp"/></label><div className="wide"><SubmitButton>Отправить обращение</SubmitButton></div></form></div></>;}


import { createReportAction } from '@/app/actions';
import LocationFields from '@/components/location-fields';
import { requireUser } from '@/lib/auth';
import { typeLabel } from '@/lib/labels';
export default async function ReportForm() { await requireUser('RESIDENT'); return <><div className="page-heading"><div><p className="eyebrow">НОВОЕ ОБРАЩЕНИЕ</p><h1>Сообщить о проблеме</h1><p className="subtle">Укажите место и опишите ситуацию</p></div></div><div className="panel max-w-3xl"><form action={createReportAction} className="panel-body form-grid"><label className="field">Категория<select name="type">{Object.entries(typeLabel).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select></label><label className="field">Название<input name="title" required minLength={4} placeholder="Например, утечка воды во дворе"/></label><label className="field wide">Описание<textarea name="description" required placeholder="Что произошло? Есть ли опасность для людей?"/></label><label className="field">Адрес<input name="address" required placeholder="Актау, 12 мкр., дом 5"/></label><label className="field">Микрорайон<input name="district" placeholder="12 мкр."/></label><LocationFields/><label className="field wide">Фото (JPG, PNG, WebP до 5 МБ)<input name="photo" type="file" accept="image/jpeg,image/png,image/webp"/></label><p className="subtle wide">Дата и время обращения сохраняются автоматически при отправке.</p><div className="wide"><button className="button" type="submit">Отправить обращение</button></div></form></div></>; }

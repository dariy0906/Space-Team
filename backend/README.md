# Archived prototype

This directory is an unused early prototype, not part of the running application.
The supported application is the root Next.js project (`src/`, `prisma/`).
Do not deploy this prototype or share its generated Prisma client with the root app.
Its independent dependency tree uses `latest` ranges and has not been certified by
root lint, build, security or runtime checks. See `../AUDIT.md` for remaining risks.
